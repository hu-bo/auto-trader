version: '3.8'

services:
  nats:
    image: nats:latest
    container_name: exchange-sync-nats
    ports:
      - "15002:4222"
    command: [ "-js", "--user=exchange_sync", "--pass=123456" ]
    healthcheck:
      test: ["CMD", "nats-server", "--help"]
      interval: 10s
      timeout: 5s
      retries: 3
networks:
  shared_network:
    external: true
