# Copy this file to .env or .env.local and fill in your credentials.
# All values are read by the Go examples under ./example via a minimal .env loader.

# Binance REST key pair (required for trade/ws examples)
BINANCE_API_KEY=your-binance-key
BINANCE_API_SECRET=your-binance-secret

# OKX REST key pair (required) + passphrase
OKX_API_KEY=your-okx-key
OKX_API_SECRET=your-okx-secret
OKX_PASSPHRASE=your-okx-passphrase

# Toggle simulated mode (true skips real order placement)
SIMULATED=true

# Toggle demo net / demo trading mode
DEMONET=true

# Optional: request timeout in milliseconds
TIMEOUT=15000

# Optional: proxy settings (may not be wired to all transports yet)
PROXY=
SOCKS_PROXY=

